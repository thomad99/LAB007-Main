# Web-Alert Environment Configuration
# Copy this file to .env.local and fill in your actual values

# Database Configuration
DB_HOST=localhost
DB_USER=your_db_user
DB_NAME=your_db_name
DB_PASSWORD=your_db_password
DB_PORT=5432

# Email / SMTP — used for alert emails and SMS (email-to-SMS gateway)
# Prefer SMTP_* so one config works across LAB007 services
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_gmail_app_password
SMTP_FROM=your_email@gmail.com
# Optional fallback names: EMAIL_USER, EMAIL_PASSWORD or EMAIL_PASS
EMAIL_FROM=your_email@gmail.com

# Optional: Chrome executable path for Puppeteer
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome

# Environment
NODE_ENV=development
